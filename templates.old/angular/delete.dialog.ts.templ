/*
*   Do not edit this file, this file is generated by the gencrud.py module
*/
import { MAT_DIALOG_DATA, MatDialogRef } from '@angular/material';
import { Component, Inject } from '@angular/core';
import { BaseDialog } from '../../common/dialog';
import { ${cls}DataService } from './service'

@Component({
    selector: 'app-${name}-delete-dialog',
    templateUrl: './delete.dialog.html',
    styleUrls: ['../../common/dialog.scss']
})
export class Delete${cls}Dialog extends BaseDialog
{
    constructor( dialogRef: MatDialogRef<Delete${cls}Dialog>,
                 @Inject( MAT_DIALOG_DATA ) public data: any, 
                 dataService: ${cls}DataService )
    { 
        super( dialogRef, dataService )
        return;
    }

    onCancelClick(): void 
    {
        this.dialogRef.close();
        return;
    }

    onSaveClick(): void 
    {
        this.dataService.deleteRecord( this.data.record );
        return;
    }
}

